pipeline {
    agent any
    stages('Init') {
        steps {
            echo "Testing..."
        }
    }

    stages('Build') {
        steps {
            echo "Building..."
        }
    }

    stages('Deploy') {
        steps {
            echo 'Code deployed'
        }
    }
}